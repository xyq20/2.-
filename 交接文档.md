# 京东平台自动填写 - 技术交接文档

## 当前进度（2026-09-11）

**全平台“只保存”已完成组合验证；本轮没有执行任何铺货。**

- 主流程 `output/kuaimai/runs/20260911-100201/`：基础资料、抖音、淘宝、天猫、拼多多、微信小店、小红书、有赞均保存成功；京东首次因“材质”重复表单项在页面重渲染后消失而失败
- 修复后仅续跑京东 `output/kuaimai/runs/20260911-101024/`：保存成功，并重新打开页面完成持久化回读
- 上述结果是“主流程 + 京东续跑”的组合结果，不代表同一进程一次性无中断跑完全部平台
- 本轮未执行铺货

### 已确认成功

- 类目：服饰内衣 > 男装 > 男士休闲裤 > 男士休闲直筒裤
- 品牌：NEIGBORL
- 产地、裤型、流行元素、弹力、上市时间、适用人群、腰型、版型
- **风格二级级联**：Excel `休闲风/时尚都市` → 页面 `休闲风 > 简约风`
- SKU 属性表头「批量设置」已点击；首次弹窗显示「暂无属性」时点「刷新数据」，等「厚度」出现后选「常规」
- SKU 属性刷新已由 `/jd/getCategoryProperties.json` 与 DOM 交叉校验
- 京东保存后重新打开回读：5 行 SKU，京东价 `586`，库存 `100`，京东价/市场价汇总均为 `586.00`，发货时效 `48小时发货`
- 已修复“材质”重复嵌套表单项在重渲染后消失的问题：按逻辑字段去重，并在序号变化后重新绑定唯一同名字段
- 单测 `tests/test_jd_form_listing.py`：27 项通过（含单色、双色图片分组及重复字段重渲染）

### 当前失败

京东当前无已知保存失败。验证凭证：

- 全平台主流程：`output/kuaimai/runs/20260911-100201/run.log`
- 主流程阶段结果：`output/kuaimai/runs/20260911-100201/all-platform-result.json`
- 京东续跑日志：`output/kuaimai/runs/20260911-101024/run.log`
- 京东保存后回读：`output/kuaimai/runs/20260911-101024/jd-after-save-validation.json`

### 运营最终确认的图片流程（真页已跑通）

1. 点 **商品图片** 旁的 `一键追加到所有sku图`
2. 点 **商品长图** 旁的 `一键追加到所有sku长图`（文案只多一个「长」）
3. 逐个颜色（例如「军绿色」）定位其下的 **商品展示图** 和 **规格长图**
4. 只删除颜色分组内「商品展示图」的**第一张**；本次由 6 张变为 5 张
5. 回读该颜色的两组图片都是 5 张且保持同一源图序号顺序
6. 不删除顶部公共商品展示图（本次仍为 5 张），不点任何「一键覆盖」

不要点「一键覆盖到所有SKU图」。覆盖按钮会出现两次，文案还带大写 `SKU`。

### 定位约束

不要再用「商品图片」当唯一分区名。改为点两段**互不相同**的按钮全文：

- `一键追加到所有sku图`
- `一键追加到所有sku长图`

父节点套了同样文案时，只点最内层节点。`.sc-upload` 是单张图片槽位，整组必须用 `.muti-upload` 定位。不能再按页面纵向位置选第一组；必须用每个颜色旁的「使用商品图片」作为锚点，按 DOM 顺序把该颜色的「商品展示图/规格长图」配成一对。颜色数、参考 SKU 图数、页面图片组数不一致时立即停止。

```bash
./run.command --platform jd --no-save \
  --excel-url "smb://gongxiang/共享文件/谭/products/战浮+NGBL-2064/产品信息.xlsx"
```

预览或保存前截图应在 `output/kuaimai/runs/<timestamp>/`：

- `jd-style-before-save.png`
- `jd-images-before-save.png`（显示颜色分组的商品展示图/规格长图数量与顺序）
- `jd-before-save.png`

---

## 项目概述

快麦 ERP 商品自动化脚本的京东资料模块。类目、属性、价格、发货时效、SKU 图片以及只保存后的重新打开回读已验证。独立 `--platform jd` 时不改基础资料。本轮未执行正式铺货。

## 核心实现

### 1. 级联选择器（风格）

- 文件：`jd_form_listing.py`
- 方法：`_open_cascader()`、`_visible_cascader_options()`、`_click_cascader_option()`、`_select_cascader_values()`
- 容器是 `.el-cascader`，菜单是 `.el-cascader-menu`，选项是 `.el-cascader-node`
- Excel 斜杠是同级 OR：`休闲风/时尚都市`
- Excel 逗号是级联路径：`休闲风,简约风`
- 二级页真实候选是 `简约风 / oversize`。`JD_CASCADER_LEAF_ALIASES` 把休闲风、休闲、时尚都市兼容到 **简约风**
- 2026-09-08 18:41 真页已选中 `休闲风 > 简约风`

### 2. 字段值映射

文件：`taobao_listing.py`，常量 `TAOBAO_VALUE_ALIASES`。

```python
(normalize_label("版型"), normalize_option("直筒")): ("直筒型",),
(normalize_label("风格"), normalize_option("休闲风")): ("休闲",),
(normalize_label("产地"), normalize_option("中国大陆")): ("中国",),
(normalize_label("弹力"), normalize_option("无弹")): ("无弹力",),
(normalize_label("弹力"), normalize_option("无弹力")): ("无弹",),
```

京东风格叶子另有 `JD_CASCADER_LEAF_ALIASES`，不要和淘宝「休闲」混用。

### 3. 价格字段

`fill_summary_prices()` 对京东价、市场价使用 `numeric=True`，因为页面会把 `586` 格式化成 `586.00`。

### 4. SKU 属性

`apply_sku_thickness()`：精确点击 SKU 属性表头下的「批量设置」。首次弹窗无属性时必须「刷新数据」，等 API 和 DOM 同时出现厚度后选择「常规」；缺少入口时报错，不允许静默跳过。

### 5. 商品图片（已跑通）

`append_sku_images()`：精确点两个「一键追加」全文 → 用颜色锚点配对各组 `.muti-upload` 商品展示图/规格长图 → 只删除每个颜色商品展示图的第一张 → 按本地 SKU 参考图分类并回读两组数量/源图顺序。顶部公共商品展示图只回读、不修改。

## 测试命令

```bash
./run.command --platform jd --no-save \
  --excel-url "smb://gongxiang/共享文件/谭/products/战浮+NGBL-2064/产品信息.xlsx"
```

```bash
python3 -m unittest tests.test_jd_form_listing
```

2026-09-11 验证结果：

- `python3 -m unittest tests.test_jd_form_listing`：27 项通过
- `python3 -m unittest tests.test_launchers`：24 项通过
- `python3 -m unittest tests.test_kuaimai_erp.ExecutionModeTests`：18 项通过

## 关键文件

1. `jd_form_listing.py` — 京东主逻辑（级联、SKU、价格、一键追加）
2. `taobao_listing.py` — 字段别名、选项规范化、Excel 斜杠/逗号解析
3. `youzan_form_listing.py` — 表单项定位基类
4. `tests/test_jd_form_listing.py` — 风格级联和图片回显单测
5. `kuaimai_erp.py` — `--platform jd` 入口；预览截图 `jd-style-before-save.png`、`jd-images-before-save.png`

## 已知限制

- 类目搜索建议由 Element UI 传送到弹窗外的 `<body>`；定位已兼容弹窗内类目树和页面级搜索建议，仍必须先有 JSON 唯一目标才点 DOM
- 级联目前按二级验收；更深层级现有循环可扩展
- 双色已用浏览器 DOM 回归和合成颜色图通过；当前真实商品只有「军绿色」一种颜色，尚需一个真实双色产品做最终页面验收。颜色无法可靠分类时会停止，不猜测。
- 京东只保存及重新打开回读已通过；正式铺货尚未执行
- 全平台主流程与京东续跑是两次运行的组合验证，尚未完成一次完整、不中断的全平台只保存回归
