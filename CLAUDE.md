# 京东平台自动填写需求

## 目标

在现有快麦 ERP 商品自动化脚本中增加“京东资料”填写和保存校验。测试商品为 `NGBL-10588`，所有商品字段以现有产品 Excel 为数据源。

## 执行原则

1. 使用脚本自己启动的浏览器，不接管用户本地浏览器。
2. 单独运行京东时，不填写基础资料，直接进入“京东资料”。
3. 页面结构、类目和属性候选以 API/JSON 数据为主依据，DOM 仅用于辅助点击和可见回读。API 与 DOM 不一致时必须停止，不能猜选。
4. 下拉框有唯一精确候选时选中该候选；没有匹配但控件允许自定义输入时，直接输入 Excel 值并回读。
5. 批量设置只点击一次，设置后必须逐行回读，避免价格或库存被重复写入。
6. 本阶段只填写和保存京东资料，不执行铺货。

## 1. 修改商品类目

1. 进入“京东资料”，点击“修改类目”。
2. 搜索并选择完整类目路径：

   `服饰内衣 > 男装 > 男士休闲裤 > 男士休闲直筒裤`

3. 必须先从类目 API/JSON 中确认唯一目标，再通过 DOM 点击同名候选。
4. 点击后同时确认目标行已勾选，且弹窗底部“已选”路径已更新，然后才点击“确定”。
5. 确定后回读页面完整类目路径，不能仍保留旧类目“男士休闲哈伦裤”。

## 2. 基本信息与商品参数

1. 品牌填写 `NEIGBORL`。页面为空时优先使用“一键应用品牌配置”，并回读确认。
2. 货号根据 Excel 填写，测试商品为 `NGBL-10588`。
3. 产地根据 Excel 填写，当前值为“中国大陆”。
4. 商品毛重根据 Excel 克重换算为公斤。当前 `430g` 填为 `0.43kg`；页面显示 `0.430` 也应判定为正确。
5. 其他基本字段根据 Excel 填写。

## 3. 商品属性

1. 所有商品属性根据 Excel 填写。
2. 当前 Excel 对应的主要字段包括：

   - 面料：棉，京东枚举中对应“棉布”。
   - 材质：棉（100%）。
   - 裤长：长裤。
   - 裤型/款式：根据 Excel 候选顺序精确匹配。
   - 流行元素：口袋/多口袋。
   - 图案：纯色。
   - 弹力：无弹。
   - 腰型：中腰。
   - 适用季节：四季通用。
   - 裤门襟：拉链。
   - 版型：直筒。
   - 工艺：水洗。
   - 功能：耐磨。
   - 品牌类型：时尚潮牌。
   - 上市时间、适用人群、适用场景、裤脚口款式等：根据 Excel 填写。

3. “材质”初始可能只显示“添加”按钮。必须先点击“添加”，等待材质下拉和百分比输入框出现，然后选择“棉”并填写 `100`。
4. Excel 面料/材质带百分比时才填百分比，没有百分比时保持空白。
5. 京东属性选择后会异步重新渲染。每次填写下一个字段前，重新获取当前 API/DOM 结构，不要继续使用过期的 `nth()` 定位。

## 4. SKU 规格明细

1. 根据 Excel 填写顶部“京东价”和“库存”。当前测试数据为：京东价 `586`，库存 `100`。
2. 填完后点击价格/库存区域的“批量设置”。
3. 回读每一行 SKU，确认京东价和库存均已正确填入。京东价必须大于 0，库存必须是非负整数。
4. 点击 SKU/属性列头下的“批量设置”。
5. 在弹窗中将“厚度”选择为 `常规`，点击“确定”，然后回读确认。

## 5. 其他京东字段

1. 页面底部“京东价（元）”和“市场价（元）”根据 Excel 填写。当前测试数据均可填 `586`。
2. 发货时效选择 `48小时发货`。
3. 其他京东字段根据 Excel 填写。
4. 如果页面将“搜索推荐图”标为必填，使用页面的“批量设置搜索推荐图”，仅选用本商品现有的 3:4 主图，并回读每一行 SKU。

## 保存与验收

1. 先运行京东填写预览，确认所有字段和 SKU 批量设置无误，不保存。
2. 预览通过后执行“仅保存，不铺货”。
3. 保存必须获得页面或保存接口 JSON 的真实成功凭证，不能只看点击动作或 Toast。
4. 保存后使用纯只读方式回读并校验：类目、品牌、货号、商品毛重、商品属性、SKU 京东价、SKU 库存、SKU 厚度、底部京东价/市场价、发货时效。
5. 回读函数严禁补写或修改任何值。只有所有回读结果与 Excel/固定规则一致，才能报告京东保存成功。

## 截图参考

- `/var/folders/0r/9lfzmk110g95hmhpx8bz13bw0000gn/T/codex-clipboard-7166b33f-aa9e-4a80-ae3b-919aa84082ce.png`
- `/var/folders/0r/9lfzmk110g95hmhpx8bz13bw0000gn/T/codex-clipboard-d1c183d7-bcfc-4140-8043-45161c6d543f.png`
- `/var/folders/0r/9lfzmk110g95hmhpx8bz13bw0000gn/T/codex-clipboard-f319ccfb-46ef-4ace-aec9-23c8c843e733.png`
- `/var/folders/0r/9lfzmk110g95hmhpx8bz13bw0000gn/T/codex-clipboard-02c48b2e-e8de-4d92-9f1c-3123f7ad7065.png`
- `/var/folders/0r/9lfzmk110g95hmhpx8bz13bw0000gn/T/codex-clipboard-82f6d661-c329-4bb9-8c41-bf3b78bad455.png`
- `/var/folders/0r/9lfzmk110g95hmhpx8bz13bw0000gn/T/codex-clipboard-8c44432f-cbda-418a-b271-6d8a42c54adc.png`
- `/var/folders/0r/9lfzmk110g95hmhpx8bz13bw0000gn/T/codex-clipboard-fb9450ed-dbbf-4d1a-a4e8-1ad2acfefb10.png`

